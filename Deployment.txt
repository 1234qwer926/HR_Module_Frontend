git log --oneline
git reset --hard e9d1b89    
git restore .
git clean -fd


# exit venv if active
deactivate

# remove old venv folder (be careful!)
Remove-Item -Recurse -Force .\myenv

# create a fresh venv using your current user/python
python -m venv myenv

# activate and reinstall deps
.\myenv\Scripts\Activate.ps1
python -m pip install --upgrade pip
# install dependencies
python -m pip install "uvicorn[standard]" fastapi
# or: python -m pip install -r requirements.txt
python -m uvicorn main:app --reload






Deployment for the Spring Boot Backend

docker --version
mvn clean install
create a file Dockerfile in main directory 

            update the docker file with

FROM openjdk:17-jdk-alpine

WORKDIR /app

COPY target/dtect-springboot-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8081

CMD [ "java", "-jar", "app.jar" ]

opean the docker desktop to start docker 

docker build -t java-application .

docker images

docker run -p8081:8080 -d java-application

docker tag java-application-pulselms:latest pavan50872/java-application-pulselms

docker login

docker push java-application-pulselms or push from the docker hub(before pushing create the name of image in the repo of hub)


cat based questions (parameters) need to updated in the batabase  -Done(change the recalibration to 10 from 1 after testing)
hr video completion after submit the exam complete --Done
video recordings update path to the s3 bucket   -Done

bulk cat questions upload end point -Done
bulk video questions upload endpoint -Done
bulk applications upload endpoint   -Done
POST /cat-items - Create a new CAT question -Done   
PUT /cat-items/{item_id} - Update an existing CAT question -Done
DELETE /cat-items/{item_id} - Delete a CAT question -Done
keep the time limit to auto complete the testing -idea is to add the encryped time in the url and decode 
                                                    it in the frontend and keep the validation for the button
bulk video evaluations -Done
authentication

Frontend Major Changes
    ai recognization model need to update in the frontend for cat based exam
    and speech to text and text to speech convertions need to update in the frontend for the hr video exam  -Done 

    keep the time limit to auto complete the testing -idea is to add the encryped time in the url and decode 
                                                    it in the frontend and keep the validation for the button

    video hr Evaluation frontend page for the hr  --need changes like url of video and keep the evaluation in table format


public users

@app.post("/jobs", response_model=JobResponse, status_code=status.HTTP_201_CREATED)
@app.get("/jobs/{job_id}", response_model=JobResponse)
@app.post("/applications", response_model=ApplicationResponse, status_code=status.HTTP_201_CREATED)
@app.post("/upload-to-s3")
@app.post("/cat/start", response_model=CATExamStartResponse)
@app.post("/cat/next-item", response_model=CATItemResponse)
@app.post("/cat/submit-answer", response_model=CATAnswerResponse)
@app.post("/cat/complete", response_model=CATExamResults)
@app.put("/start-exam/{email}/{hr_video_exam_key}")
@app.post("/video-responses/batch", response_model=List[VideoResponseDetail], status_code=status.HTTP_201_CREATED)



authentication
authorization


--I want to Implement the basic auth from the .env file in the backend

make it perfect with the mail templete different url for 2 tests
Frontend ui changes at managing video questions
Frontend ui for the video evaluation  
frontend bulk appplication ui for admin -Done
just dont update the cat percentile 




bulk applications upload excel endpoint  --Done

update instructions in the cat exam -done

catdata for bulk upload -Done
make change to recalibration to 10 and user need to take 30 questions or exit based on user ability  -Done
navigation should not be there for some user exam login paths   -Done
<Route path="/exam/login" element={<ExamLogin />} />
        <Route path="/exam" element={<CATExam />} />
        <Route path="/exam/complete" element={<ExamComplete />} />
<Route path="/hr-video-exam" element={<HRVideoExamLogin />} />